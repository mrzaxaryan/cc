@inherits Microsoft.AspNetCore.Components.ComponentBase

<button class="c2-btn @VariantClass @SizeClass @CssClass"
        @onclick="OnClick"
        @onclick:stopPropagation="StopPropagation"
        disabled="@Disabled"
        title="@Title"
        type="button">
    @if (Loading)
    {
        <span class="c2-spinner c2-spinner-inline"></span>
    }
    @ChildContent
</button>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public bool Loading { get; set; }
    [Parameter] public string? Title { get; set; }
    [Parameter] public string? CssClass { get; set; }
    [Parameter] public bool StopPropagation { get; set; }
    [Parameter] public BtnVariant Variant { get; set; } = BtnVariant.Default;
    [Parameter] public Size Size { get; set; } = Size.Sm;

    private string VariantClass => Variant switch
    {
        BtnVariant.Primary => "c2-btn-primary",
        BtnVariant.Success => "c2-btn-success",
        BtnVariant.Danger => "c2-btn-danger",
        BtnVariant.DangerSolid => "c2-btn-danger-solid",
        BtnVariant.Warning => "c2-btn-warning",
        BtnVariant.Ghost => "c2-btn-ghost",
        BtnVariant.Link => "c2-btn-link",
        _ => ""
    };

    private string SizeClass => Size switch
    {
        Size.Xs => "c2-btn-xs",
        Size.Md => "c2-btn-md",
        Size.Lg => "c2-btn-lg",
        Size.Icon => "c2-btn-icon",
        _ => ""
    };
}
