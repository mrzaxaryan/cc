@inherits Microsoft.AspNetCore.Components.ComponentBase

<div class="c2-progress @SizeClass @CssClass">
    <div class="c2-progress-fill @VariantClass" style="width:@Percent%"></div>
</div>

@code {
    [Parameter] public int Percent { get; set; }
    [Parameter] public string? CssClass { get; set; }
    [Parameter] public ProgressVariant Variant { get; set; } = ProgressVariant.Info;
    [Parameter] public Size Size { get; set; } = Size.Sm;

    private string VariantClass => Variant switch
    {
        ProgressVariant.Success => "c2-progress-success",
        ProgressVariant.Warning => "c2-progress-warning",
        ProgressVariant.Danger => "c2-progress-danger",
        ProgressVariant.Muted => "c2-progress-muted",
        _ => "c2-progress-info"
    };

    private string SizeClass => Size switch
    {
        Size.Xs => "c2-progress-xs",
        _ => ""
    };
}
