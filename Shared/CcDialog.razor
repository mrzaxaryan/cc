@inherits Microsoft.AspNetCore.Components.ComponentBase

@if (Visible)
{
    <div class="cc-dialog-backdrop" @onclick="Cancel"></div>
    <div class="cc-dialog">
        <div class="cc-dialog-header">
            <span class="cc-dialog-title">@Title</span>
            <button class="cc-dialog-close" @onclick="Cancel">&times;</button>
        </div>
        <div class="cc-dialog-body">
            @ChildContent
        </div>
        <div class="cc-dialog-footer">
            <CcBtn OnClick="Cancel">Cancel</CcBtn>
            <CcBtn Variant="BtnVariant.Primary" OnClick="Confirm">@ConfirmText</CcBtn>
        </div>
    </div>
}

@code {
    [Parameter] public bool Visible { get; set; }
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string ConfirmText { get; set; } = "OK";
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public EventCallback OnConfirm { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private async Task Confirm() => await OnConfirm.InvokeAsync();
    private async Task Cancel() => await OnCancel.InvokeAsync();
}
